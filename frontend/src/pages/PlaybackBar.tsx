// Compatibility shim for checkouts that still contain an older PlaybackBar
// under src/pages. The real implementation lives in src/components.
export { PlaybackBar } from '../components/PlaybackBar'
